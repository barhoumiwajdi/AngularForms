export class User {
    constructor(
        public name: string,
        public email: string,
        public tel: number,
        public topics: string,
        public timing: string,
        public subscribe: boolean
    ) { }
}
